version: 2.1

orbs:
  python: circleci/python@0.3.2

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - run:
          name: Run Tests
          command: python3 -m unittest discover tests
          
workflows:
  main:
    jobs:
      - build-and-test
